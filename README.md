Sources for the plugin `plugin.ouya.xiaomi`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.ouya.xiaomi" = {
            publisherId = "tv.ouya",
        },
    },
}
```
